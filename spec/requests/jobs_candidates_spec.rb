require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/jobs_candidates", type: :request do
  
  # This should return the minimal set of attributes required to create a valid
  # JobsCandidate. As you add validations to JobsCandidate, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      JobsCandidate.create! valid_attributes
      get jobs_candidates_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      jobs_candidate = JobsCandidate.create! valid_attributes
      get jobs_candidate_url(jobs_candidate)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_jobs_candidate_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "renders a successful response" do
      jobs_candidate = JobsCandidate.create! valid_attributes
      get edit_jobs_candidate_url(jobs_candidate)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new JobsCandidate" do
        expect {
          post jobs_candidates_url, params: { jobs_candidate: valid_attributes }
        }.to change(JobsCandidate, :count).by(1)
      end

      it "redirects to the created jobs_candidate" do
        post jobs_candidates_url, params: { jobs_candidate: valid_attributes }
        expect(response).to redirect_to(jobs_candidate_url(JobsCandidate.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new JobsCandidate" do
        expect {
          post jobs_candidates_url, params: { jobs_candidate: invalid_attributes }
        }.to change(JobsCandidate, :count).by(0)
      end

      it "renders a successful response (i.e. to display the 'new' template)" do
        post jobs_candidates_url, params: { jobs_candidate: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested jobs_candidate" do
        jobs_candidate = JobsCandidate.create! valid_attributes
        patch jobs_candidate_url(jobs_candidate), params: { jobs_candidate: new_attributes }
        jobs_candidate.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the jobs_candidate" do
        jobs_candidate = JobsCandidate.create! valid_attributes
        patch jobs_candidate_url(jobs_candidate), params: { jobs_candidate: new_attributes }
        jobs_candidate.reload
        expect(response).to redirect_to(jobs_candidate_url(jobs_candidate))
      end
    end

    context "with invalid parameters" do
      it "renders a successful response (i.e. to display the 'edit' template)" do
        jobs_candidate = JobsCandidate.create! valid_attributes
        patch jobs_candidate_url(jobs_candidate), params: { jobs_candidate: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested jobs_candidate" do
      jobs_candidate = JobsCandidate.create! valid_attributes
      expect {
        delete jobs_candidate_url(jobs_candidate)
      }.to change(JobsCandidate, :count).by(-1)
    end

    it "redirects to the jobs_candidates list" do
      jobs_candidate = JobsCandidate.create! valid_attributes
      delete jobs_candidate_url(jobs_candidate)
      expect(response).to redirect_to(jobs_candidates_url)
    end
  end
end
